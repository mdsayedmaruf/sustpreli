# QueueStorm Investigator — environment variables.
# The service runs fully WITHOUT any of these set (deterministic, no DB, no API key).

# CORS: comma-separated origins, or * for all.
CORS_ORIGINS=*
